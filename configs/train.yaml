defaults:
  - model: vjepa
  - data: default
  - training: default
  - _self_

experiment_name: "vjepa_train"
log_dir: "logs"
device: "cuda"
seed: 42

clearml:
  project_name: "pamiq-vision-exp"
  task_name: ${experiment_name}
