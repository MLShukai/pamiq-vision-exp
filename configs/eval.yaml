defaults:
  - shared: default
  - model: vjepa
  - data: default
  - evaluation: default
  - _self_

experiment_name: "vjepa_eval"
checkpoint_path: ???
encoder_key: "context_encoder"  # Key in model dict for the encoder to evaluate
device: "cuda"
seed: 42
