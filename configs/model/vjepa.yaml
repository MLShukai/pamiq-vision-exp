# V-JEPA model configuration
_target_: exp.models.vjepa.create_video_jepa
video_shape: [16, 224, 224]  # [T, H, W]
tubelet_size: [2, 16, 16]
in_channels: 3
hidden_dim: 768
embed_dim: 128
depth: 6
num_heads: 12
predictor_hidden_dim: null  # defaults to hidden_dim // 2
predictor_depth: null  # defaults to depth
predictor_num_heads: null  # defaults to num_heads // 2
